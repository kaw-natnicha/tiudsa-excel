What I did
- use UNIQUE to find the cetagories in each column
- use SUMIF to find total sales by product category
- use COUNTIF to count the number of transactions per region
- use AVERAGEIF to find average order value per customer segment (by city, the only available one)
- added a Size column then use IFS formula to classify each order as "Small", "Medium", or "Large" based on value thresholds (up to 1000, between 1001 and 2000, and 2001 and over)
